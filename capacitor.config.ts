import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dermaplanner.vsp.b3dev',
  appName: 'b3dev_clinica_vsp_app_v2',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;
